Place static files for `daf_base` docs, such as images and sample data, in this directory.

They can be referenced in reStructuredText using absolute paths, e.g.:

```
.. image:: /_static/daf_base/image.jpg
```
