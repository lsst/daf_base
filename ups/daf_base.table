setupRequired(utils >= boost.python)
setupRequired(pex_exceptions >= boost.python)

envAppend(LD_LIBRARY_PATH, ${PRODUCT_DIR}/lib)
envAppend(DYLD_LIBRARY_PATH, ${PRODUCT_DIR}/lib)

envAppend(PYTHONPATH, ${PRODUCT_DIR}/${LSST_PYTHON_PREFIX})
